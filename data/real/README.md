# Fresh football data

This directory is populated by `scripts.rebuild_fresh_football` or the GitHub Actions workflow.

Source URLs point to the public `datasets/football-datasets` Premier League CSVs, themselves sourced from Football-Data.co.uk. Generated CSVs are intentionally absent from the release until the reproducible workflow runs.
