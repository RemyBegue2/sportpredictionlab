# Application de la V4.6.0

Depuis un dépôt V4.5.0 propre :

```bash
git apply sportpredictionlab-v4.6.0.patch
git add .
git commit -m "Add calibration feature lab and compact cockpit"
git push
```

Puis lancer `Deploy production` avec `verify_browser=true`.
