[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -lc 'cd /app && PYTHONPATH=/app python -m scripts.run_shadow_cycle'"
cronSchedule = "*/15 * * * *"
restartPolicyType = "NEVER"
