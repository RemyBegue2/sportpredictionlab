[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -lc 'cd /app && PYTHONPATH=/app python -m scripts.sync_daily_predictions --horizon-days 31'"
cronSchedule = "0 */6 * * *"
restartPolicyType = "NEVER"
