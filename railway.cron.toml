[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -lc 'cd /app && PYTHONPATH=/app python -m scripts.sync_current_odds --football soccer_epl'"
cronSchedule = "*/15 * * * *"
restartPolicyType = "NEVER"
