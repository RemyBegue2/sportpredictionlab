[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m scripts.sync_current_odds --football soccer_epl"
cronSchedule = "*/15 * * * *"
restartPolicyType = "NEVER"
